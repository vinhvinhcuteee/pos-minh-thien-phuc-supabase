name: Backup Supabase to Google Drive

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client
          
      - name: Create timestamp
        run: echo "TIMESTAMP=$(date +'%Y%m%d_%H%M%S')" >> $GITHUB_ENV
          
      - name: Backup database (direct URL)
        run: |
          # Dùng trực tiếp URL, không cần parse
          pg_dump "${{ secrets.SUPABASE_DB_URL }}" > "backup_${{ env.TIMESTAMP }}.sql"
          echo "Backup completed"
          
      - name: Compress backup
        run: |
          gzip "backup_${{ env.TIMESTAMP }}.sql"
          ls -la
          
      - name: Install rclone
        run: |
          curl -fsSL https://rclone.org/install.sh | sudo bash
          
      - name: Create rclone config
        run: |
          mkdir -p ~/.config/rclone
          echo '${{ secrets.GDRIVE_SA_JSON }}' > ~/.config/rclone/sa.json
          cat > ~/.config/rclone/rclone.conf << EOF
          [gdrive]
          type = drive
          scope = drive.file
          service_account_file = ~/.config/rclone/sa.json
          EOF
          
      - name: Upload to Google Drive
        run: |
          rclone copy "backup_${{ env.TIMESTAMP }}.sql.gz" gdrive: --drive-root-folder-id "${{ secrets.GDRIVE_FOLDER_ID }}" -v
          
      - name: Clean old backups
        run: |
          find . -name "backup_*.sql.gz" -mtime +30 -delete
